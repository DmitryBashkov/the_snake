# TODO: довести игру до рабочего состояния

## Блокеры запуска

- [ ] Исправить синтаксические ошибки во всех модулях. В `core/mechanics.py`, `core/renderer.py`, `objects/snake.py` и `objects/game.py` не закрыты скобки импортов и/или выражений; в `core/renderer.py` не закрыты вызовы `pygame.draw.rect`. Пока эти ошибки есть, импорт `the_snake` завершается `SyntaxError`.
- [x] Устранить конфликт импортов в `the_snake.py`: оставить один импорт `pygame` и удалить `from core.mechanics import pygame`.
- [x] Исправить создание часов в `core/game.py`: использовать `pygame.time.Clock()`, а не `pygame.Clock()`, которого в API pygame нет.
- [ ] Инициализировать pygame до создания `Renderer`. Сейчас `Game()` создаёт `Renderer`, а тот вызывает `pygame.display.set_mode`; это допустимо только после `pygame.init()`. Сохранить такой порядок и не создавать `Game` при импорте модуля.

## Точка входа и основной цикл

- [ ] Экспортировать из `the_snake.py` классы `GameObject`, `Snake`, `Apple`, константы поля и направления, а также глобальные `screen` и `clock`: это требуется приложенными тестами. Альтернатива — заменить тесты на тесты реального публичного API, если текущие тесты не являются требованием задания.
- [ ] Сделать один источник кадровой синхронизации. Сейчас `main()` вызывает `clock.tick(20)`, а `Game.turn()` вызывает второй `tick`; оставить `tick` только в одном месте.
- [ ] В основном цикле вызывать `handle_keys(snake)` до шага игры. Сейчас события pygame вообще не обрабатываются, поэтому нельзя менять направление и закрывать окно.
- [ ] Использовать результат `game.turn()`: завершать цикл после `False`, либо реализовать `while game.running`.
- [ ] Вызвать `pygame.quit()` при нормальном выходе из игрового цикла.

## Модель объектов

- [ ] Определить единый контракт `GameObject`: нужны конструктор без обязательных параметров, `position`, `body_color`, `draw`, а также корректная реализация/отказ от абстрактного `move`. Сейчас `GameObject` абстрактный, поэтому его нельзя создать, а приложенный тест создаёт его напрямую.
- [ ] Согласовать имена `position` и `positions`. Сейчас объекты хранят `_positions` и предоставляют `positions`, а тесты ожидают `position`; код столкновений и отрисовки должен использовать единый формат координат.
- [ ] Реализовать `draw()` у `GameObject`, `Snake` и `Apple` либо перенести отрисовку целиком в `Renderer` и синхронно изменить тесты. Сейчас метода `draw` нет.
- [ ] Добавить `randomize_position()` в `Apple`. Сейчас определён только защищённый метод `_randomize_position()` в базовом классе, а ожидается публичный метод яблока.
- [ ] В `Snake` реализовать `get_head_position()` и `reset()`, которые требуются текущими тестами и нужны для перезапуска игры.
- [ ] Определить свойства `direction` и `position` у `Snake`; сейчас есть только `_direction` и `_positions`.

## Движение и столкновения

- [ ] Исправить алгоритм `Snake.move()`. Сейчас вся змейка смещается на один шаг, поэтому тело не следует за головой. Новый кадр должен добавлять новую голову по направлению, а хвост удаляться, если в этом ходе не было роста.
- [ ] Исправить самопересечение: сравнивать голову только с телом без головы (`positions[1:]`), иначе змейка всегда пересекается сама с собой.
- [ ] В `Game.__init__()` добавить змейку и первоначальное яблоко в `_game_objects`, иначе `turn()` не перемещает и не рисует змею и на поле нет цели.
- [ ] Перемещать змею до проверки столкновения или вычислять следующую позицию заранее. Сейчас `_has_interception()` вызывается до движения, поэтому столкновение определяется на предыдущем кадре.
- [ ] Реализовать границы поля: либо завершать игру при выходе головы за `0 <= x < SCREEN_WIDTH` и `0 <= y < SCREEN_HEIGHT`, либо корректно переносить голову на противоположный край.
- [ ] Не изменять set во время обхода и не использовать имя `object` для переменной. Откладывать удаление допустимо, но после удаления необходимо создавать новое яблоко.
- [ ] Реализовать `_game_over()`: остановить игру, очистить/перерисовать поле, сбросить змейку и объекты либо вывести экран Game Over; выбрать один сценарий и покрыть его тестом.

## Яблоки и стены

- [ ] В `Apple.__init__()` вызвать `detect_type()` либо задать начальный тип. Иначе при первом обращении к `apple.aid`, `rotten`, `hot` или `drunk` возникает `AttributeError: _type`.
- [ ] Генерировать яблоко только на свободной клетке, исключая тело змеи, стены и другие яблоки.
- [ ] Исправить диапазоны в `_randomize_position()`: `randint(0, GRID_WIDTH)` и `randint(0, GRID_HEIGHT)` могут породить координаты `SCREEN_WIDTH`/`SCREEN_HEIGHT` за границей. Использовать `randrange(GRID_WIDTH)` и `randrange(GRID_HEIGHT)` либо верхнюю границу `GRID_WIDTH - 1`/`GRID_HEIGHT - 1`.
- [ ] Реализовать время жизни стен или удалить поле `life_time`. Сейчас `Wall.move()` пуст, `life_time` не уменьшается, а стены никогда не удаляются.
- [ ] Исправить обработку гнилого яблока: при длине 1 `throw_half()` возвращает/оставляет пустые сегменты, а `Wall` получает неоднозначное состояние. Явно определить поведение для длины меньше 2.
- [ ] Исправить `reverse_snake()`: вызвать `self._positions.reverse()`, а не получать ссылку на метод без вызова.
- [ ] Формализовать эффекты `aid`, `drunk`, `hot` и `rotten`. В частности, сейчас `aid` только инвертирует направление, хотя комментарий говорит об отмене эффекта `drunk`; `hot` не увеличивает скорость.

## Отрисовка

- [ ] Исправить синтаксис `Renderer._draw_element()` и закрыть вызовы `pygame.draw.rect`.
- [ ] Перед рисованием каждого кадра очищать экран цветом `BOARD_BACKGROUND_COLOR`, затем рисовать все активные объекты и один раз вызывать `pygame.display.flip()`/`update()`. Иначе остаются следы старых позиций.
- [ ] Проверить координатную систему: все позиции объектов должны быть пиксельными координатами, кратными `GRID_SIZE`; `pygame.Rect` тогда создаётся корректно.

## Тесты и качество

- [ ] Сначала добиться прохождения `python -m pytest -q`; сейчас тесты не смогут импортировать проект из-за синтаксических ошибок и отсутствующих экспортов/методов.
- [ ] Добавить поведенческие тесты: движение головы и хвоста, запрет разворота на 180 градусов, поедание обычного яблока, каждый специальный тип яблока, столкновение со стеной, с собой и с границей, появление яблока на свободной клетке.
- [ ] Исправить `setup.cfg`: ключ должен быть `max-line-length`, а не `max-line-lenght`; добавить запятые/раздельные строки в `exclude`, если требуется формат flake8.
- [ ] Запустить `python -m flake8` после исправления синтаксиса и устранить ошибки стиля и неиспользуемые импорты.
- [ ] Дополнить `README.md`: установка зависимостей, команда запуска (`python the_snake.py`), команда тестов, описание управления и правил специальных яблок.

## Рекомендуемый порядок выполнения

1. Исправить синтаксис, импорты и создание `Clock`.
2. Сформировать публичный API, необходимый тестам, и добиться успешного импорта.
3. Реализовать корректные `GameObject`, `Snake`, `Apple` и первичную инициализацию объектов в `Game`.
4. Исправить основной цикл, обработку событий, движение и очистку/отрисовку кадра.
5. Реализовать столкновения, границы, поедание яблок, генерацию нового яблока и Game Over.
6. Реализовать и протестировать специальные яблоки и ограниченный срок жизни стен.
7. Завершить тестами, flake8 и документацией.
